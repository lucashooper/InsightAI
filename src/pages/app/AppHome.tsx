import React from 'react';
import App from '../../App';

/**
 * Main authenticated app page.
 * 
 * Renders the existing App component which contains:
 * - Sidebar
 * - Diary editor
 * - Dashboard
 * - Settings
 * - Playbook
 * - etc.
 */
const AppHome: React.FC = () => {
  return <App />;
};

export default AppHome;
